name: go

on:
  push:
    branches: [ main ]
jobs:
  build-lin:
    runs-on: ubuntu-latest
    steps:
    - name: Build-lin
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
      go-version: '>=1.17.0'
    - run: go build -x -v 

  build-win: 
    runs-on: windows-latest
    steps:
    - name: Build-win
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
      go-version: '>=1.17.0'
    - run: go build -x -v 

  build-mac:
    runs-on: macos-11
    steps:
    - name: Build-mac
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
      go-version: '>=1.17.0'
    - run: go build -x -v 
